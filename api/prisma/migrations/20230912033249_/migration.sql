/*
  Warnings:

  - You are about to drop the column `index` on the `vendor_serving` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE `vendor_serving` DROP COLUMN `index`;
